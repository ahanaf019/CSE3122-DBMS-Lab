use db;

create table students(
sid int,
s_name varchar(30),
fees int,
team varchar(1)
);

insert into students values
(1, 'ees', 200, A),
(2, 'qees', 300, B),
(3, 'sas', 100, B),
(4, 'e1es', 500, A),
(5, 'e2es', 420, C);